<?xml version="1.0" encoding="UTF-8"?>
<view name="po-indexing-view" template="po-tpl.xml" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

	<labels default="en">
		<locale name="en">
		</locale>
	</labels>

	<section-overrides>
	</section-overrides>

	<property-overrides>
	</property-overrides>

	<lineItem-overrides>
	</lineItem-overrides>

	<action-overrides>
	</action-overrides>

	<hidden-overrides>
		<hidden name="processInformation.response" defaultValue="Complete"/>
	</hidden-overrides>

	 <documentRejection>
		<rejectionReasons>
			<reason name="Reject" commentsRequired="true" note="message.returnException">
				<subReason name="Duplicate invoice"/>
				<subReason name="Not a GenuineInvoice"/>
				<subReason name="Terminate"/>
				<subReason name="Waiting Receipts"/>
				<subReason name="Other"/>
			</reason>
			<reason name="Expense Invoice" commentsRequired="true" note="message.expenseInvoice">
			</reason>
			<reason name="Rescan" commentsRequired="true" note="message.rescan">
			</reason>
			<reason name="Duplicate" commentsRequired="true" note="message.duplicatereivew">  </reason>
		</rejectionReasons>
	</documentRejection>

	<validations>
		<validation name="validation.duplicateInvoiceCheck" messageType="warning" note="message.duplicateReviewInvoiceValidation"/>
		<validation name="validation.validVendor" messageType="warning" note="message.validVendorValidation"/>
		<validation name="validation.accountCode" />
		<validation name="validation.duedate.check" />
		<validation name="validation.validPaymentTerm"/>
	</validations>
</view>
